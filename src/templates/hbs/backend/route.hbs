const express = require('express');
const router = express.Router();
const {{ controllerFileName }} = require('../controllers/{{ controllerFileName }}');
{{#if authMiddleware}}
const { authenticate } = require('../middlewares/auth');
{{/if}}

/**
 * Rutas para {{ modelName }}
 */

// Obtener todos los {{ modelName }}s
router.get('/', {{#if authMiddleware}}authenticate, {{/if}}{{ controllerFileName }}.getAll);

// Obtener un {{ modelName }} por ID
router.get('/:id', {{#if authMiddleware}}authenticate, {{/if}}{{ controllerFileName }}.getById);

// Crear un nuevo {{ modelName }}
router.post('/', {{#if authMiddleware}}authenticate, {{/if}}{{ controllerFileName }}.create);

// Actualizar un {{ modelName }} existente
router.put('/:id', {{#if authMiddleware}}authenticate, {{/if}}{{ controllerFileName }}.update);

// Eliminar un {{ modelName }}
router.delete('/:id', {{#if authMiddleware}}authenticate, {{/if}}{{ controllerFileName }}.delete);

module.exports = router;