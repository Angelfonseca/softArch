const express = require('express');
const router = express.Router();
const {{modelName}}Controller = require('../controllers/{{controllerFileName}}');
{{#if authMiddleware}}
const { authenticate } = require('../middleware/auth');
{{/if}}

/**
 * Rutas para {{modelName}}
 */

// Obtener todos los {{modelName}}
router.get('/', {{#if authMiddleware}}authenticate, {{/if}}{{modelName}}Controller.getAll);

// Obtener un {{modelName}} por ID
router.get('/:id', {{#if authMiddleware}}authenticate, {{/if}}{{modelName}}Controller.getById);

// Crear un nuevo {{modelName}}
router.post('/', {{#if authMiddleware}}authenticate, {{/if}}{{modelName}}Controller.create);

// Actualizar un {{modelName}} existente
router.put('/:id', {{#if authMiddleware}}authenticate, {{/if}}{{modelName}}Controller.update);

// Eliminar un {{modelName}}
router.delete('/:id', {{#if authMiddleware}}authenticate, {{/if}}{{modelName}}Controller.delete);

// [IA_GENERATED_CODE]

module.exports = router;